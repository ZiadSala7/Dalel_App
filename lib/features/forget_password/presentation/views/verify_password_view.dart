import 'package:dalel_app/features/forget_password/presentation/views/widgets/verify_password_view_body.dart';
import 'package:flutter/material.dart';

class VerifyPasswordView extends StatelessWidget {
  const VerifyPasswordView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: VerifyPasswordViewBody(),
    );
  }
}
